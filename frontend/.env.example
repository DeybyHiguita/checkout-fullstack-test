# URL pública del backend propio
VITE_API_URL=http://localhost:3000/api/v1

# Llave PÚBLICA de la pasarela (única llave que puede ir al navegador,
# se usa para tokenizar la tarjeta sin que el PAN pase por el backend propio).
VITE_GATEWAY_PUBLIC_KEY=
VITE_GATEWAY_BASE_URL=
